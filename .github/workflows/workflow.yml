name: "Custom Action"
on:
  push: 

jobs:
  main:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - name: Call action
        uses: ./actions/local-action
        with:
          who-to-greet: jesus
