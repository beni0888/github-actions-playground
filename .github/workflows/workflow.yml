name: "Custom Action"
on:
  push:

env:
  FOO: foo
  BAR: bar

jobs:
  main:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - name: Call action
        uses: ./.github/actions/custom-action
        with:
          who-to-greet: jesus
          a-secret: ${{secrets.SO_SECRET}}
  next:
    needs:
      - main
    runs-on: ubuntu-latest
    steps:
      - run: env
      - run: |
          echo "SECRET: ${A_SECRET}" 
