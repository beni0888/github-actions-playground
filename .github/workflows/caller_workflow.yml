name: "Compose workflow"
on:
  push:

jobs:
  job1:
    runs-on: ubuntu-latest
    steps:
      - run: echo "This is the first job in the workflow"

  reusable_workflow:
    uses: ./.github/workflows/reusable-workflow.yml
    with:
      message: "Testing secret leak"
    secrets:
      json_doc: ${{secrets.SAMPLE_GCLOUD_KEY}}
